description: >
  Sample description:
# What will this job do?

executor: default

parameters:
  to:
    type: string
    default: "World"
    description: "Hello to whome?"
steps:
  - greet:
      to: << parameters.to >>